MCP OAuth consent fix for Usable-hosted deployments.

## Fixed

- The MCP OAuth consent page renders on `AUTH_MODE=usable` deployments. The authenticated shell gated on hosted mode only, so `/oauth-consent` showed a blank screen and every MCP login timed out waiting for the callback.

Full Changelog: https://github.com/every-app/open-seo/compare/v0.1.10...v0.1.11
