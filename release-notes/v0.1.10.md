MCP OAuth sign-in fix for Usable-hosted deployments.

## Fixed

- MCP clients can complete an OAuth login on `AUTH_MODE=usable` deployments. The authorize endpoint now resolves the session through the shared auth-mode resolver instead of the hosted-only one, so it no longer answers 500 "Missing Better Auth hosted configuration".

Full Changelog: https://github.com/every-app/open-seo/compare/v0.1.9...v0.1.10
