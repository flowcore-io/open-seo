Self-host database health fix.

## Fixed

- Self-host deployments keep working past the first request: the Postgres client is now created per request, so queries no longer fail with "Cannot perform I/O on behalf of a different request" and `/api/health` reports a healthy database.

Full Changelog: https://github.com/every-app/open-seo/compare/v0.1.8...v0.1.9
